#define FOR(i,N) for(int i=0 ; i<N ; i++)

#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <set>

using namespace std;

int main(){
    

    while(1){
        int A, B;
        cin >> A >> B;
        if(A == 0 && B ==0) break;
        cout << A+B << "\n";
    }
    return 0;
}